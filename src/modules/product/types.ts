export enum TrackingType {
  SIMPLE = 'SIMPLE',
  EXPIRABLE = 'EXPIRABLE',
  SERIALIZED = 'SERIALIZED',
  LOT_TRACKED = 'LOT_TRACKED',
  VARIANT = 'VARIANT',
}

export enum UnitOfMeasure {
  PCS = 'PCS',
  KG = 'KG',
  LITER = 'LITER',
  BOX = 'BOX',
}
